import { SpurigkeitPipe } from './spurigkeit.pipe';

describe('SpurigkeitPipe', () => {
  it('create an instance', () => {
    const pipe = new SpurigkeitPipe();
    expect(pipe).toBeTruthy();
  });
});
