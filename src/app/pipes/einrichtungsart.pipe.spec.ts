import { EinrichtungsartPipe } from './einrichtungsart.pipe';

describe('EinrichtungsartPipe', () => {
  it('create an instance', () => {
    const pipe = new EinrichtungsartPipe();
    expect(pipe).toBeTruthy();
  });
});
