import { BusverkehrPipe } from './busverkehr.pipe';

describe('BusverkehrPipe', () => {
  it('create an instance', () => {
    const pipe = new BusverkehrPipe();
    expect(pipe).toBeTruthy();
  });
});
